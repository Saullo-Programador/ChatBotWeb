services:
  - type: web
    name: chatbot-service
    plan: free
    buildCommand: npm install
    startCommand: npm start
    runtime: node
    envVars:
      - key: NODE_ENV
        value: production
